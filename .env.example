# Copy to .env.development (local) or .env.production, or inject as real env
# vars in production. All are validated at boot (src/config/env.validation.ts).

# Postgres connection
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=localdb
DB_USERNAME=postgres
DB_PASSWORD=password
DB_SSL=false

# App
PORT=3000
# NODE_ENV=development
